<?xml version="1.0" encoding="utf-8"?>

<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ProgressBarTest.CustomProgressBar">
    <ContentView.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <BoxView
                x:Name="ProgBarBg"                
                Color="{StaticResource SecondaryDark}"
                HorizontalOptions="Fill"/>
            <BoxView
                x:Name="ProgBar"
                Color="{StaticResource SuccessLight}"
                VerticalOptions="Fill"
                HorizontalOptions="Start"/>
            <Label x:Name="LblValue" TextColor="White" FontSize="12" FontAttributes="Bold" VerticalOptions="Center" HorizontalOptions="Center" />
        </Grid>
    </ContentView.Content>
</ContentView>